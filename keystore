## Create a key store for signing apks

```
keytool -genkey -v -keystore tarn.keystore -alias tarn -keyalg RSA -keysize 2048 -validity 10000
keytool -importkeystore -srckeystore tarn.keystore -destkeystore tarn.keystore -deststoretype pkcs12
```
